/*#ifndef FIGHT_H
#define FIGHT_H

#include "Utils.h"
#include "Estat.h"

class Fight : public Estat
{
public:
    Fight(sf::RenderWindow *gaemWindow, sf::RenderTexture *gaemTexture);
    ~Fight();
    void update(sf::Time elapsedTime);
    void handlePlayerMouse(mouseButtons mouseBut, sf::Vector2f mouseClick);

private:

};

#endif // FIGHT_H
*/
